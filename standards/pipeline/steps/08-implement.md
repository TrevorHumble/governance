# 08: Implement

Spawn `agents/implementation-agent.md`, tier per `standards/pipeline/templates/model-tiers.md`,
with the passing issue and every prior-art path it needs. The implementer satisfies each
acceptance criterion, confirms API signatures against real documentation, and returns the two
required handoff fields: `Duplicated-ownership self-check:` and `Caused-defect surfacing:`. A
handoff missing either is an incomplete artifact.
