- 2026-08-21 - #10 the local em-dash check now sees uncommitted work, so a dirty tree can no longer false-green (PR #18). `scripts/check-emdash.js` diffed only `origin/<default>...HEAD`; it now also collects the working tree and each untracked non-ignored file, parses each diff separately (concatenating would let a line deleted in a commit wrongly exempt the same line re-added uncommitted), and merges results de-duplicated on file and line. The revert escape suppresses only the committed range. Issue review FAILed then passed on rewrite. Code review round 1 FAILed on a blocker: the untracked scan false-greened on any filename git prints in quoted form, because `git diff --no-index` returns the same exit status 1 for "could not access" as for "differences found". Round 2 FAILed on a new major the fix introduced: in a repo with no commit yet, a file added to the index then edited on disk was scanned by neither the staged diff nor the untracked scan. Round 3 PASSed; design-philosophy gate PASSed; briefing audit PASSed all three rounds. Recorded omissions, named in `WHAT-IT-CHECKS.md` and `DESIGN.md`: binary-classified files stay invisible, the untracked scan is capped at 2000 files and fails loud past it, and a filename carrying a quote, backslash, or control character still prints in git's escaped form. CI expectation: green, and unchanged in judgment, since a CI checkout is clean and has no untracked files.
