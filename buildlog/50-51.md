- 2026-08-27 - #50 The unchanged-artifact exemption, and inherited approval for an epic's children (PR #51). Companion to TrevorHumble/TaskMasterWedding#1296, split along the ownership line because a child's pre-commit hook blocks parent paths and governance is a pull rather than a push. Records two standing owner directives the shared charters previously contradicted: a repo's declared pre-review process file may define an exemption under which a change on the declared surface does not re-enter the live owner loop, and an epic the owner approved may pre-approve the children inside its scope. The trigger was stated as an absolute in five places and the merge gate in five more, so an exempted change could not have shipped at all; the merge rule now has one home in the exemption paragraph and the other sites point at it. `agents/reviewer-pr.md` gained the duty producing the per-file evidence the exemption rests on, and refuses rather than skips where no surface is declared, so an empty enumeration can never stand in for verification. The inherited line is keyed `Inherited-approval:`, sharing no prefix with the approval marker, and only the exact `Owner-approved: yes` string grants a reviewer any exemption; a new issue-reviewer row tests the inherited line rather than trusting it. `definition-of-done.md` clause 9 and its plain-language restatement are deliberately untouched, an exempted change failing their condition rather than contradicting it, with that reading recorded in `DESIGN.md`. Issue review PASS round 7; three code lenses returned fourteen findings, all fixed, then a scoped re-check PASSed and its one minor and four nits were fixed before commit.
