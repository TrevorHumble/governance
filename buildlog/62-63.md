- 2026-08-31 - #62 The pipeline checklist and the twelve step packets (PR #63). Child A of epic #61: the build pipeline's step rules restructured into standards/pipeline/ (PIPELINE.md, one numbered checklist under 120 lines; twelve self-contained step chunks; four shared templates), every old home reduced to a one-line pointer, PIPELINE.md added to arrivesAsStructure so a child's first delivery is withheld until handled, DESIGN.md recording the component. Motivated by observed drift (skipped issue reviews and pre-reviews, a wrong-format hand-off, a hidden hand-off): agents follow rules handed to them at the step and drop rules living deep in long cross-referenced files. Landed as four sequentially reviewed rounds, each measured inside the review-size bound; issue review PASS at round 6; fourteen gating reviewer dispatches plus a briefing audit per round, every round bound to and re-verified against a git write-tree oid; recorded omissions: the 1a/1b split, the unbuilt 09-pr-review-detail.md, the plan amendment keeping the review-size bound whole in the protocol. CI green expected on this branch before merge.
