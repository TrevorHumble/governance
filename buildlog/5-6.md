- 2026-08-19 - #5 Made the shared set safe to land in a child, six defects the first sync would have tripped on (PR #6): WHAT-IT-CHECKS.md moved from sharedPaths to excludedPaths with retired left empty on purpose (a tombstone would prune a child's true copy or warn on it forever), since every repo's copy documents that repo's own CI; required_status_checks.strict flipped to $false with the tool's header rewritten to describe the payload it actually sends, recording the owner's 2026-07-17 decision reconfirmed 2026-08-19 under DESIGN.md's unchanged "Branch-protection strictness" heading and keeping the profile-field route for a future true; .githooks/commit-msg now probes powershell or pwsh, invokes the resolved launcher at both call sites, and falls through to its named BLOCKED message rather than aborting silently under set -e, covered by tests/commit-msg.test.js adopted from flatland with its launcher pick changed so the suite cannot skip itself on a pwsh-only host; the unused caught binding in scripts/check-emdash.js dropped for a child's zero-warning scripts lint; five shared markdown files reformatted for a child's required prettier --check; .gitattributes corrected to name the commit-msg hook. Issue review: 4 rounds to PASS (missing criterion for the .gitattributes defect, a criterion leaving the tool header contradicting its payload, a smallest-edit instruction leaving a false pointer inside a child, a plan step naming the probe but not its two call sites, and a wrong provenance citation, the strict comment coming from wedding commit da754b8 for #587 rather than PR #1257). Implementation review: round 1 measured 210 lines inside the 400 bound, reviewer-pr PASS plus design-philosophy and architecture returning 4 majors (silent set -e abort in the hook, stale DESIGN.md hazard 7, two disagreeing homes for the sync-guarantees statement, three disagreeing launcher probes letting the branch-protection assertion skip), all fixed and confirmed RESOLVED by a fresh scoped re-check; briefing audit PASS on both rounds. Widening recorded on both Touches copies under Finding disposition 1: standards/governance-sync.md became the single home for the sync statement with WHAT-IT-CHECKS.md and CLAUDE.md pointing at it, and tests/ps-launcher.js the single home for launcher resolution across five suites. Recorded omissions: wedding needs no separate issue for the same header defect (parent-owned file, corrected on adoption); a formatter or eslint step in this repo's own CI stays parked per the owner, with seven repo-wide prettier failures all in excludedPaths; apply-branch-protection.ps1 has still never run live, and running it against this repo's own unprotected main is the owner's next step. Gates at ship: npm test 124/124 across 8 files, check-emdash exit 0, prettier --check clean over the sharedPaths markdown set. CI expectation: the build workflow green on PR #6.
