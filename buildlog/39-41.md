- 2026-08-23 - #39 every reviewer now asks whether the work needed to be this big (PR #41). The design standard's six review questions and six red flags all asked one thing, is this built well, so a reviewer had one direction available to it and every finding it could make grew the thing under review; the worked case is this repo's own issue #28, a bot flagging issues opened without `needs-issue-review`, filed reviewed implemented and closed without anyone asking whether that failure had ever happened, and it had not, nineteen of nineteen issues here carried the label at creation and the bot's only firing across fifty-one issues in two repos was a standing sync-anchor issue a human waves through. Three questions now sit in `standards/adversarial-review-protocol.md`, necessity, cost of carry, and sizing, placed there rather than in `standards/design-philosophy.md` because eight of the nine reviewer charters close their read lists against that standard so a pointer would have reached nobody, and because that placement is what reaches a doc-only change, which skips the design-philosophy gate but is still gated by the PR reviewer. Two named rows, `unforced complexity` and `ghost gate`, give an oversize finding something to cite, and alone among the eight rows they do not take the precedence carve-out: they are judgment calls, so a finding citing either states a concrete failure scenario and is downgraded without one, since otherwise the rows would be an automatic non-downgradable major the reviewer owes no evidence for, the exact failure the change exists to stop. The YAGNI limit is stated once beside the questions so a refactor, rename, simplification, or test that buys future change is never findable under either row, and both rows ship with a worked Flag/Clean pair and a `Not a finding:` guard. Issue review took five rounds, four FAILs: a row planned without its guard, three acceptance-criteria clauses with no delivering step, and on round 4 the blocker that reshaped the design, that the charters may not open the file the plan wanted to point them at. Code review round 1: reviewer-pr PASS, reviewer-design-philosophy FAIL, reviewer-architecture FAIL, both failing lenses independently finding the same root cause, that the questions reached every reviewer but the brakes did not; the briefing audit invalidated the first PR briefing for a hand-added path in a machine-generated scope list and that reviewer was re-briefed and re-run. Round 2 scoped re-check PASSed both lenses, round 3 a fresh PR review against the final tree PASSed, briefing audit PASS and round valid all three rounds, with every minor and nit fixed inline per the one-round stop rule. Recorded omissions: the Dependabot auto-merge path gets no necessity question, declared out of scope on evidence since this repo has merged zero Dependabot pull requests so the failure has no recorded instance and the gate would itself match `ghost gate`; no `DESIGN.md` entry for the judgment-row exemption, raised as a minor by the architecture lens and left with the rationale in the standard; and nothing mechanical enforces any of it, it holds by instruction. Gates at ship: npm test 202 passed across 12 files, lint, format:check, and check:emdash all clean. CI expectation: the `build` check green on PR #41.
