- 2026-08-28 - #53 Narrowed the governance-sync rule that made any manifest difference a structure verdict, so a strictly additive, non-colliding manifest change now reaches a child as an ordinary content sync instead of halting every unrelated path behind it (PR #54). The exception is bounded to four fields (sharedPaths, excludedPaths, classes, arrivesAsStructure); classesDefault and retired still force structure on any difference, addition included, because a first classesDefault reclassifies every path no classes entry names and an added retired entry deletes a child's file. Within those four, nothing may be removed and no existing entry's value may change, compared ordinally; excludedPaths is compared as an ordered prefix rather than a set because it is gitignore-style and a reorder can flip a live exclusion, and a !-prefixed addition never qualifies; and no entry newly added to sharedPaths or classes may collide with a path the child already tracks, judged against git ls-files rather than the on-disk tree so a gitignored runtime directory raises no phantom collision. Prompted by TaskMasterWedding#1314, where three added lines that could take nothing away from that child withheld all seventeen planned paths. Merge-on-green is unchanged and is exactly what the exception is bounded to protect. Issue review: FAIL, FAIL, then PASS on round 3, DENY sonnet-only; the two FAILs caught that the additive test omitted retired, that a prefix/** addition could swallow a child's whole directory, that a first classesDefault was unguarded, and that the plan named the wrong test file. Code review: six rounds, all three gating reviewers PASS on round 5, briefing audit PASS on every round. Rounds caught, each reproduced by measurement rather than accepted on report: an empty or absent classes object reading as one phantom key in PowerShell, which silently voided the feature for any child whose manifest predates classes; a pure excludedPaths reorder riding the exception and flipping the live buildlog/** against !buildlog/README.md pair; Start-Process -ArgumentList splitting the repo path on spaces, so the tracked-file reader returned $null for every repo under Code Projects and disabled the feature in production while CI stayed green; a case-insensitive -ne in the ordered comparison; and five documentation statements this change falsified across DESIGN.md and both standards. The last two fixes carry mutation-proven guards: reverting either now fails a named test. CI went red on PR #54 after round 5, on tests/governance-sync.test.js, expected IsNull=False Count=3 to be IsNull=False Count=2 (run 33156187575): git ls-files -z NUL-terminates rather than NUL-separates its output, and on Linux Start-Process's redirect appends a trailing newline, so a two-file child tree returned three entries; fixed in round 6 by cutting at the last NUL instead of splitting and filtering blanks. A scoped re-check after that fix then found this diff had itself turned Get-ManifestDiffFields's existing case-insensitive comparison into a gate: a case-only retired difference no longer even reached the four-field allowlist check, silently voiding the guard rounds 3 and 4 built; also fixed in round 6, with the same ordinal comparison already used elsewhere in the file, mutation-proven. Recorded omission: the top-level whole-manifest equality gate in Get-SyncClassification is separately case-insensitive, so a manifest differing only by letter case still reads there as no difference at all; that half is pre-existing, unchanged by this diff, carried to the owner as a report note. Gates at ship: npm test 298 passed across 14 files, npm run lint, npm run format:check, and npm run check:emdash all clean. CI: red on PR #54 after round 5, green after the round 6 fix.
