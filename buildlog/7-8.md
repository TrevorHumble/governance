- 2026-08-20 - #7 Stopped CI running every change twice and finishing runs a newer push had already superseded (PR #8): the push trigger narrowed from every branch to branches [main], replacing the mutually-exclusive branches-ignore key rather than joining it, and a concurrency block keyed on the workflow name plus the pull request number with cancel-in-progress true now kills a superseded PR run, while non-PR events fall back to github.run_id so every push to main and every merge-queue entry sits alone in a group of one, distinct groups rather than a cancel exemption. Ported from TrevorHumble/orchestrator-template#5 merged 2026-08-19, where the same two blocks took a head commit from two runs to one. WHAT-IT-CHECKS.md and a ci.yml comment both described the old trigger set and justified the em-dash gate's pull_request guard by a branch-push run this change removes; both now state the true reason, that a direct push to main has no pull request and so offers no merge-base diff of added lines to judge. Cost counted honestly in the issue and PR rather than the filed "roughly half": a one-push PR goes from three runs to two, a third off, with half the asymptote on a busy PR. Issue review: PASS with DENY sonnet-only, seven minor and nit findings folded into the issue body before implementation, including a bare cross-repo #5 autolinking to this repo's own issue 5, two further stale passages the plan had missed, the overclaimed saving, and a verification step that named no file and would have tripped the commit-msg hook. Code review round 1: reviewer-pr PASS and reviewer-design-philosophy PASS with no blocker and no major, briefing audit PASS with the round valid at bound tree oid 798078b and 20 lines measured against the 400-line bound; the minor findings both gates raised, a six-line comment over the trap-prong five-line budget and a rationale that restated the guard as its own reason in two files, were fixed inline before the commit per the one-round stop rule. Recorded omissions: a CI_RUNNER variable and a workflow-shape guard test both stay out per the issue's own out-of-scope section and the owner's over-engineering bar; the run-count observation itself cannot run before this merges, so #7 stays open with no closing keyword in the commit or the PR body until a throwaway doc-only PR confirms one run per head commit and a cancelled predecessor, with both run ids recorded there. Gates at ship: npm ci clean, npm test 124 passed across 8 files, check-emdash no added em dashes. CI expectation: the build workflow green on PR #8.
