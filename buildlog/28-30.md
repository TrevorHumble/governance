- 2026-08-23 - #28 The parent now runs the issue-lifecycle and dependency-update machinery it requires of every child (PR #30). Adds `.github/workflows/issue-guard.yml`, which comments on and labels an issue opened without `needs-issue-review` and closes or blocks nothing, and `.github/dependabot.yml` on the `github-actions` and `npm` ecosystems, weekly at directory `/`; both ported from the children, with the dependabot pointer retargeted to `DESIGN.md` hazard 3 and the script hardened to attempt both API calls even if the first throws. Three governing-artifact files asserted things the change falsifies and were corrected with it: `CLAUDE.md` said eight required labels, `WHAT-IT-CHECKS.md` said no Dependabot workflow was configured, and `DESIGN.md` hazard 3 said this repo does not port `dependabot.yml` at all. Issue passed adversarial review after three rounds: round 1 caught the missing `WHAT-IT-CHECKS.md` on `Touches`, round 2 caught the missing `DESIGN.md` and a plan that would have auto-closed the issue before its behavioral criteria had evidence. Code review round 1: reviewer-pr PASS, reviewer-design-philosophy PASS, reviewer-security PASS (advisory, dispatched at orchestrator discretion for the `issues: write` grant on an externally triggerable event), reviewer-briefing PASS with the round confirmed valid; no blocker or major, and all minor and nit findings fixed in place before commit, including a dangling pointer at `.claude/rules/dependencies.md` that does not carry the rule it was cited for. Decisions recorded: only `unverified-issue` is provisioned, at colour `e99695` rather than the child's `d93f0b`, which this repo already uses twice, while `dependencies`, `javascript`, and `github_actions` are left to Dependabot and deliberately uncounted, since nothing here reads them and counting them would go stale on the first Dependabot run. No manifest change needed, confirmed by running the exactly-one-of assertion with both new files present rather than assuming it. Recorded omissions: acceptance criteria 1 and 2 are behavioral and cannot run before merge, since a workflow fires only from the default branch's copy of itself, so PR #30 carries no closing keyword and they are verified against two throwaway issues afterwards and recorded on the PR body before #28 is closed by hand. Gates at ship: npm test 191/191, lint exit 0, format:check clean, check-emdash exit 0, plus a `node --check` syntax check of the workflow's extracted inline script. CI expectation: the `build` job green on PR #30.
