- 2026-08-22 - #16 harvested 5 genuine local improvements flatland made to its copy of this governance machinery before its first sync (PR #25). `agents/reviewer-architecture.md`'s duplicate-component scan and the architecture-lens auto-trigger list now both also cover `.claude/commands/` and `tools/` / "command, or tool", in all three homes of each. `standards/skill-standards.md`'s banned-word checklist now points to `standards/documentation-standards.md` § "Anti-AI-slop" as the sole owner instead of hardcoding a second copy. `tools/setup-hooks.ps1` is now the single owner of the `core.hooksPath` arming call; `.claude/hooks/session-greeting.ps1` dot-sources it and only calls it when arming is actually needed. `standards/issue-standards.md`'s in-license check regained "Free local tooling is in scope." Issue review FAILed (4 major: all-presence-check ACs with no behavioral value; two of the three homes of each duplicated list left uncovered by the criteria; a vague implementation-plan step) then PASSed on rewrite. Round 1 (PR + design-philosophy) both FAILed: PR reviewer found the hook's unconditional dot-source of `tools/setup-hooks.ps1` failed the greeting closed if that file were ever missing, even in the steady already-armed state; design-philosophy found the same banned-word list still hardcoded, uncaught, in two sibling files (`agents/reviewer-skill.md`, `agents/reviewer-documentation.md`) the skill-standards.md fix's new "sole owner" claim did not actually hold for, plus a `Set-HooksPath` helper whose three different return shapes let a caller silently misread an arming failure as success. All three majors fixed; `Touches` widened per Finding disposition 1 to record the two additional files. Round 2 scoped re-check (both reviewers plus briefing audit) confirmed all three majors fixed, and the fix itself introduced new minor/nit findings (a misleading not-armed message, an unredirected-stdout hardening gap, a `$ErrorActionPreference = 'Stop'` throw path in the native git call) fixed inline with no re-review, per the one-round stop rule. Briefing audit PASSed both rounds, round valid, no scope mismatches; its only findings were advisory notes on the round-2 briefings naming mechanisms instead of goals. Recorded omission, named in the issue: `agents/reviewer-issue.md:70` keeps a second, un-updated home of the in-license check's out-of-license text, left alone as a separable single-source-of-truth pass. CI expectation: green (`build` check), confirmed before merge.
