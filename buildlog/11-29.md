- 2026-08-22 - #11 The ownership map and standard shelves become law, fan-out Step 2 (PR #29): shipped `standards/ownership-map.md` (new, syncs to every child, states routes by path class so it carries no one child's domain content), replaced `governance-manifest.json`'s `tools/**` and `scripts/**` globs with the sixteen enumerated parent-owned files, moved the manifest itself into `sharedPaths` and gave it five sidecars (`classes`, `classesDefault` defaulting an unjudged path to `structure`, `arrivesAsStructure`, `repoProfileFields`, `shelfRoots`), added `.githooks/pre-commit` with `tools/ownership-core.ps1` as the wall that rejects a child commit staging a parent-owned path, and widened `DESIGN.md`'s `criticalPaths` schema line, which is what actually closes #9. Issue review: FAIL x5 then PASS on round 6, DENY sonnet-only (governing-artifact surface); the rounds found the shipped standard about to carry one child's filenames into every other child, the `CLAUDE.md` leash ordering every child to delete the heading `Get-SyncPlan` warns about, an acceptance criterion that pinned two class-definition sentences whose merge clauses duplicated the rule `standards/governance-sync.md` declares it alone owns (amended in the issue, recorded openly), and a plan step describing an extraction of a per-path matcher out of `Resolve-SharedSet`, which is an entry-to-file expander with no such matcher in it. Code review: seven rounds across three commits plus five scoped re-checks, with the briefing audit PASSing every round and catching four real bias slips in the orchestrator's own briefings (a prior-verdict disclosure about files still under review, a minimizing "small follow-up edits", a pre-cleared index/worktree difference, and a pre-defended missing plan step). Blockers found and fixed: the wall failed OPEN because a shell fallback collapsed every PowerShell failure into an empty result the hook read as "nothing staged", so a child could commit away the wall's own engine and go unguarded from then on; a manifest that parses but carries no `sharedPaths` array silently disarmed it entirely; `Test-IsSyncBranch` matched case-insensitively so a hand-made `Issue-45-Governance-Sync-` branch was exempt; both hooks would have shipped non-executable (`core.fileMode` is false on the authoring machine, git skips a 100644 hook in silence on any POSIX clone) and, worse, an already-synced child holds `commit-msg` byte-identical so it is neither an add nor an update and nothing would ever have repaired its mode, now fixed by staging the whole `.githooks/` slice with `--chmod=+x` and covered by a test for the mode-only repair path specifically; the launcher-absent test built its sandbox PATH from git's own directory, which on `ubuntu-latest` is `/usr/bin` where `pwsh` installs, and would have reddened this PR's own CI; and `tools/setup-hooks.ps1` reported both hooks active without checking either file existed, in the exact scenario where one had not arrived. Three widenings recorded: `.githooks/commit-msg` and `.gitattributes` (falsified by a second hook existing), then `standards/governance-sync.md` (told every child a sync commit faces one commit-time gate, and recorded nothing about the branch-shape dependency that lets a sync commit through the second). One finding took three re-review rounds, the quote-escaping coverage, resolved rather than halted because it narrowed each round and the final remedy was a single line the reviewer had verified under both variants; the fixture now turns nine of thirteen cases red under a neutered escaper instead of one. Recorded omissions: a child still needs a CI guard job, which lives in child-owned `.github/` and cannot ride this PR; three of the eight repaired files are child-owned so the sync will not carry those repairs; one clause of the owner's goal is deliberately unshipped because it contradicts `standards/governance-sync.md`'s two-permanent-resolutions rule; and the staged-path read stays duplicated between `tools/issue-core.ps1`, which declares itself self-contained on purpose, and `tools/ownership-core.ps1`. Gates at ship: `npm test` (191 passed across 11 files, up from 132), `npm run lint`, `npm run format:check`, and `npm run check:emdash` all exit 0. CI expectation: the build workflow green on this PR.
