- 2026-08-28 - #53 (follow-up) Made the whole-manifest equality gate ordinal and gave the NUL-safe git read one home, the two deferrals the session-ending late-note pass overruled (PR #55). Get-SyncClassification compared the two manifests' canonical JSON with PowerShell's case-insensitive -ne, so two manifests differing only by letter case read as no difference at all: the diff branch was skipped, the run classified content, and nothing was withheld. PR #54 had just added the sentence that every field outside the additive exception's four stays structural on any difference, and this gate made that new sentence false, which is why it was fixed here rather than reported. Measured before the fix: a parent rewriting a retired path's case shipped its plan with nothing withheld; after, structure. Separately, Get-StagedParentOwnedPath and Test-StagedHasCode read NUL-separated git output through the interpolation form, which PowerShell splits on newlines and rejoins with a space; both now call one shared helper, Invoke-GitCaptureRaw, with a single justified exception in tools/issue-core.ps1, whose header declares it self-contained and whose test fixture copies only that one file. Review found three further defects the work itself caused, each fixed in this issue rather than deferred: the rewritten commit-msg reader swallowed every exception into an empty path list, which .githooks/commit-msg reads as an explicit DOC and exits 0, so a broken temp directory would have let a code commit through with no issue reference (removing the catch was not enough on its own, because under ErrorActionPreference Continue a .NET exception does not unwind the function; the preference is now Stop, proven end to end against the real hook, pre-fix exit 0 against post-fix exit 1); four statements calling the core a pure library with no side effects, two of them written in the same round and each resting a safety argument on it, left false once that file began spawning processes; and two trap warnings deleted with the old inline copy, that -ArgumentList space-joins without quoting and that -RedirectStandardError needs a real temp file because NUL is an ordinary file on a POSIX runner, each the fix for a defect that had already shipped once in this issue, now on the shared helper's own header where every caller reads them. The first attempt at a regression guard did not discriminate: four hook tests asserted only that the commit was blocked, which a mangled filename satisfies just as well since it still matches standards/**, so the guard was decorative; it is now a direct isolation test asserting the returned path byte for byte, plus a Linux-only embedded-newline case, because the byte-for-byte case only discriminates on a non-UTF-8 console and CI is ubuntu-latest. Both are mutation-proven. One further defect caught after commit: a raw 0x00 byte written into a string literal made git classify the whole test file as binary, which costs the line diff and the em-dash gate's coverage of it; the separator is now an escape. Code review: three rounds on this branch, both gating reviewers PASS on the third, every finding reproduced by measurement rather than accepted on report. Recorded omission: standards/ownership-map.md's parent-owned-paths prose list is structural but no sidecar encodes it, so the checker still cannot see edits to it; pre-existing, upheld as a genuine deferral by the late-note pass, and already recorded in standards/governance-sync.md. Gates at ship: npm test 305 passed across 14 files on CI (304 passed and 1 skipped on Windows, the Linux-only case), npm run lint, npm run format:check, and npm run check:emdash all clean. CI: green on PR #55 and on main after merge.
