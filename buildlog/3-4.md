- 2026-08-18 - #3 Shipped the governance sync machinery (PR #4): tools/governance-sync.ps1 plus -core pull shared governance from the declared governanceHome into a child as one reviewed PR, planning in a detached worktree at the child's own origin default branch (same-tree invariant), pruning retired paths only on a recorded sha256 match, and force-refreshing one branch per parent state; standards/governance-sync.md is the single operative home of the contradiction review and override rule, wired into build.md step 0b and the orchestrator operating rules; manifest retired entries became {path, sha256}, .claude/settings.json left sharedPaths, and three new guards landed (retired schema, retired/shared disjointness, citation prefix across the 11 rewritten DESIGN.md citations). Issue review: 11 rounds to PASS. Implementation review: declared-oversize round, reviewer-pr plus design-philosophy plus architecture; 4 majors (gitignore-proof staging plus three prose referent/single-home defects) fixed and verified RESOLVED by fresh scoped re-checks, one after a second bounded fix. Recorded omissions: per-child adoption deferred to follow-up issues; autocrlf can degrade a prune to retained-divergent (safe direction). Gates at ship: npm test 118/118, check-emdash exit 0. CI expectation: the build workflow green on PR #4.
