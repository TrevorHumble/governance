- 2026-09-21 - #88 Replaced the untested "Static-content-first ordering" spawn-prompt rule in
  `standards/adversarial-review-protocol.md` with issue #85's dated cache test record (three
  controlled Sonnet probes: a byte-identical prompt hits the cache in full, but a shared prefix
  followed by a differing tail earns zero credit), and dropped the now-dead caching clause from
  `standards/pipeline/templates/spawn-skeleton.md`'s assembly sentence (PR #89). Both gating
  reviewers and the briefing-audit lens PASSed round 1; one minor citation-consistency finding
  from the design-philosophy reviewer was fixed inline, no re-review required. CI (`build`)
  green in 2m25s. Closes #85, whose plan's steps 2-5 (building a shared-block cache) are
  abandoned as a result of this measurement.
