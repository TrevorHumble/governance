- 2026-08-26 - #46 Every leftover note is now challenged by someone other than the agent that wrote it before it reaches the owner (PR #48). The note channel was the one disposition nothing audited (the owner-supplied post-mortem's diagnosis: the unaudited exit is the one work drains out of), so the change puts a reader at every point work leaves: a note is written to the issue's own body under a ## Notes heading with the agent's set-aside justification (why not a caused defect, why the size rule's branches do not permit it) beside the gitignored crash-safe record; it rides under every round reviewer as the notes-under-challenge briefing field, the third sanctioned field in the protocol's De-bias section; ruling on a briefed note is a duty, not an option, in the protocol-owned vocabulary OVERRULE (binds like a blocker, routed through disposition 1 or the size rule, with a classification-contest bound so a true branch-4 note resolves to wait-or-report rather than wedging), DROP (only at the confident-drop bar owned by orchestrator rule 4, over 70 percent do-nothing, and only unanimously: any UPHOLD defeats a DROP, the orchestrator never picks between reviewers), and UPHOLD; a note no round reviewer ruled on gets the late-note pass, agents/reviewer-notes.md, a new charter whose trigger and round-counting/size-bound exemptions are single-homed in the protocol's Reviewer count by artifact bullet, dispatched at wrap, ship, or session-ending halt alike, so a halt hands the owner no unchallenged note either. Before a note may exist at all, three pre-checks run: the issue's existing notes, the owning repo's tracked owner-declines.md (append-only on the owner's answer, so he is asked once; cross-wall reads via gh api, failing toward asking), and the owning repo's open issues. The two mechanizable matches are exact on a normalized one-line substance key in tools/note-check-core.ps1 (declines parse the recorded line shape with a greedy separator so a substance containing a dash still round-trips; titles match on equality; the one sanctioned containment is the Touches-line fragment), pinned by tests/note-check.test.js, 15 cases; everything fuzzier is reviewer judgment, said plainly in WHAT-IT-CHECKS.md, with design rationale in DESIGN.md. Issue review: PASS round 1 with four minors folded in. Code round 1: reviewer-pr FAIL (3 majors: substring decline matching suppressing new notes, gh issue list default-30 truncation, the halt path skipping the pass), reviewer-design-philosophy FAIL (3 majors: the same substring defect, the late-note trigger restated in four disagreeing homes, the drop bar in two homes), reviewer-architecture FAIL (3 majors: no in-round ruling duty so silence counted as challenge, the substring defect, trigger drift; 2 minors: post-ship OVERRULE route, cross-repo declines mechanics, both addressed). Round-2 scoped re-check confirmed all seven consolidated fixes and found 1 new major (no aggregation rule for split rulings); round-3 scoped re-check PASS, briefing audit PASS on the bound tree, 1 minor and 1 nit fixed inline as a separate audited-drift commit. Recorded omissions: rounds 1 and 2 ran without a concurrent briefing audit, a process slip recorded here rather than papered over; both rounds returned FAILs that were fixed, and the accepted round 3 was audited valid. Gates at ship: npm test 275/275, format and em-dash checks clean. CI expectation: build green on PR #48.
