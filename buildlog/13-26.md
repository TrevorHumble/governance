- 2026-08-22 - #13 Added prettier and eslint as required checks in the parent's own CI (PR #26): the parent enforced neither on its own JS/Markdown while both are required checks children run over arriving shared files, so a parent commit could redden every child's sync PR at once; one shared file, scripts/check-emdash.js, was already failing prettier when the issue was authored. eslint.config.js and .prettierignore are new root files, added to governance-manifest.json's excludedPaths so neither syncs to a child, which keeps its own lint/format config. Issue review: FAIL, FAIL, then PASS on round 3, DENY sonnet-only (governing-artifact surface); round 1 found an unrecorded new tracked file with no manifest classification and an unbounded reformat-files Touches entry, round 2 found the AC3 lint claim untested against nine files outside Touches, two of which actually failed (a bare catch clause and an unused destructured var, both fixed). Code review: two full rounds, both PASS with no blocker or major; round 1's briefing audit PASS caught nothing, but the PR reviewer and design-philosophy reviewer each independently found the branch was stale against origin/main and that the merge tree would leave agents/orchestrator.md failing prettier, fixed via a recorded Touches widening and a rebase. Round 2 found seven minor and three nit findings (an eslint glob gap that would fail a correct file added outside scripts/tools/tests, a duplicated gitignore filter in .prettierignore, inconsistent CI step names, a missing format write script, an under-documented WHAT-IT-CHECKS.md entry), all fixed inline per the no-re-review rule for minor and nit findings. origin/main moved twice more during review, issue #12's merge and then issue #16's harvest PR; both required a re-rebase before the final tree matched what would actually merge. Process defect found and corrected after the fact: the PR was merged before this buildlog fragment was written and pushed on its own branch, out of the pipeline's stated order, where the fragment lands on the shipping branch so its own CI run covers it; this fragment ships instead as a small doc-only follow-up. Gates at ship: npm run format:check, npm run lint, npm test (132 passed across 8 files), and npm run check:emdash all exit 0 on the merged tree. CI expectation: the build workflow green on this fragment's own PR.
