- 2026-08-22 - #14 rule-checker: every planned sync file is classified content or structure before anything ships, so a structure change becomes a standing issue in the child instead of a machine-merged PR (PR #36). Evidence: PR reviewer PASS with all eight acceptance criteria traced to input-to-output evidence, design-philosophy PASS, architecture lens PASS, briefing audit PASS and round valid every round; 202 tests across 12 files, emdash, lint and format clean. Review caught five defects before merge, including a case-insensitive class comparison that would have machine-merged a mis-cased `Content` into every child, and a fix that put a `gh` call on the "nothing to sync" path. Recorded omissions: the `gh issue list --limit 1000` cap can miss the standing issue in a child holding more than 1000 open issues, and the ownership map's prose rule that editing its own parent-owned-paths list is structural stays uncheckable, both stated in the shipped standard. The run halted once at the impasse rule and resumed on owner direction; see this file's `[HALT]` entry in `BUILDLOG.md`. Two disposition-1 widenings recorded on the issue's Touches line. CI expected green on `build`.
