- 2026-08-30 - #59 replace the two-round impasse halt with the two-path stop condition: a formally disputed blocker/major finding routes to a fresh referee reviewer (new `agents/reviewer-referee.md`, payload-only, ruling SUSTAIN/OVERTURN grounded in the owner's settled decisions), and an undisputed defect loops fix-and-fresh-reviewer to a ceiling of eight re-review rounds (round-2 re-check counts as re-review round 1; halt at the eighth, round 9 overall) before any halt reaches the owner (PR #60). Owner settled the design in chat 2026-08-30 after the #14 halt showed the flat two-round rule abandoning a fixable defect. Issue review passed on round 4 (three FAIL rounds). PR review: round 1 ran PR, design-philosophy, and architecture reviewers, all FAIL, 15 consolidated findings including the ceiling stated with two halt points across four files, the referee charter contradicting itself on reads, the dispute path wedging on an empty round-2 diff, and no dispute channel in the implementer contract; round 2 confirmed all 15 fixed and FAILed 3 new (DESIGN.md's #14 numbering contradicting BUILDLOG.md, a caused defect from the coordinator's own fix instruction; two skeleton/charter parity gaps); round 3 confirmed all resolved, PASS, one minor (payload-only rule vs the skeleton's charter and protocol reads) fixed inline. Briefing audit PASSed with the round valid all three rounds; its advisory notes on dispatch hygiene (mechanism-naming in briefing 3 of round 1, compressed file lists in the audit payload, fix-asserting restatements in rounds 2 and 3) are recorded here. Recorded widening: `standards/agent-standards.md` added to `Touches` at round 1, the referee's ruling shape having falsified its reviewer-output sentence. Checks on the committed tree: `check:emdash`, `lint`, and `format:check` clean; tests 270 passed with 34 pre-existing failures in `tests/governance-sync.test.js` reproduced identically on clean main (environment-dependent, no `.ps1` or tool file touched by this md-only change); the declared `build` CI check is the authoritative gate, expected green on PR #60.
