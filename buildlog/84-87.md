- 2026-09-21 - #84 Name `standards/adversarial-review-protocol.md` the single owner of the two passages the split left word for word in both halves, the scope line and the lean-review rationale, with the ownership note placed outside the passages so they stay byte-identical (PR #87). Raised at PR review as a set-aside note; the late-note pass overruled it and bound it back into the issue, since both files sit on the issue's own Touches line and no branch of the size rule defers it. The first shape appended a marker claiming the other copy was identical and failed both gates, because appending a different sentence to each copy is what falsified the claim; the second shape passed with two nits, one fixed inline. CI expected green on the repo's five declared checks.
