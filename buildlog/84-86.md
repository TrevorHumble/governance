- 2026-09-21 - #84 Split `standards/adversarial-review-protocol.md` into a reviewer half, the new `standards/reviewer-conduct.md` at 360 lines holding the ten conduct sections, and a dispatch half at 343 lines holding the nine (PR #86). Every section body byte-identical to a pre-split copy except pointer repairs recorded in a ledger the check diffs against; references repaired in three sweeps, since a path grep misses both the bare section citations and the sentences that locate by position. Issue review took eight rounds; PR round 1 failed all three gates on seven converging findings, two of them blockers (a pointer repaired in the wrong direction, and a circular read set leaving the referee no resolvable protocol file), and round 2 passed on a scoped re-check with a minor and a nit fixed inline. CI expected green on the repo's five declared checks.
