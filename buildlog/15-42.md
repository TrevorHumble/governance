- 2026-08-23 - #15 Sync PRs now merge themselves on green CI with no reviewer, human or agent (PR #42): tools/governance-sync.ps1 arms the PR it just opened with gh pr merge --auto and lets GitHub merge it when the child's required checks pass, with no polling loop, no timeout logic and no daemon; the triggering build does not wait, it continues on the tree it has and the merged governance arrives at the next build. Two guards make green real rather than assumed, both added after review found them missing: auto-merge on a branch with no required check merges at once, CI or no CI, so the new Get-CiGateStatus confirms every name in the child's declared ciCheckNames is actually required on that branch before arming, and an unreadable protection object, an empty required-check list and an empty declared list each read as absent and never arm; and because a sync branch carries a whole-file snapshot of one parent commit, Invoke-SupersededSyncSweep closes and deletes superseded sync PRs before arming, on the ship path and the empty-plan exit but deliberately not at the no-sync-PR structure exit, where an older PR still carries content the child has not received. Every step of the arming path is non-fatal by contract: any failure warns and leaves the PR open for hand-merge, and -NoAutoMerge suppresses the arming alone, not the sweep. Retiring the reviewer retired the review's whole vocabulary, the contradiction review, the tracked override home, the declined-sync rule, the no-PR divergence disposition and the protocol carve-out, along with every pointer elsewhere in the tree that sent a reader to them, because a live citation to a deleted section is exactly how a child's orchestrator would fall back to the full round-1 gate this change exists to eliminate. Deleting the override home took the only way a child said it keeps a retired file on purpose, so acknowledgedDivergentPaths in the child's own repo-profile.json replaces it, repo-relative, forward-slashed, exact case, with one standing divergence issue per child refreshed in place and closed once nothing unacknowledged remains, whether every path was acknowledged or the divergence itself went away. Issue review: PASS on round 10, DENY sonnet-only, with the criteria growing from five to eight as successive rounds found the guard-not-required hole, the stale-armed-PR overwrite, the trapped state left by deleting the override home with no replacement, and seven other files carrying live citations to sections the same change deletes. Code review: reviewer-pr PASS on round 1 while reviewer-architecture and reviewer-design-philosophy FAILed on four majors between them, the PR body still listing paths the child had already acknowledged, the file-or-close divergence decision written out three times at three exits with its warning strings already drifted, and two live citations to deleted sections in CLAUDE.md and DESIGN.md; the round-2 scoped re-check PASSed all three lenses and the remaining minors shipped inline per the one-round stop rule, with the briefing audit PASS on both rounds. Two widenings recorded on the issue under Finding disposition 1: tools/governance-sync-core.ps1 took Get-UnacknowledgedDivergent because a pure filter belongs in the pure half, and tools/apply-branch-protection.ps1 took a one-line back-pointer comment because this change introduced a second reader of required_status_checks.checks. Recorded omissions: classic branch protection is the only shape the probe understands, so a ruleset-guarded child reads as unprotected and keeps hand-merging; the merge method is fixed at a merge commit; the gate cannot verify that a child's parent-owned-path CI guard job is among its declared ciCheckNames, because no such CI job is specified anywhere in this tree yet and .githooks/pre-commit, which does guard those paths locally, is not a CI check and cannot appear in ciCheckNames; and per-child activation (allow_auto_merge, branch protection, override count at zero) is separate from this authoring and gates no merge here. Gates at ship: npm test 220 passed across 12 files, lint, format:check and check-emdash all clean. CI expectation: the build workflow green on PR #42.
