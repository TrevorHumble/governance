- 2026-08-26 - #45 Runs now claim the files they work on and make small out-of-scope fixes under a size rule instead of leaving them as owner-facing notes (PR #47). The rule's one home is standards/issue-standards.md section "The file claim and the size rule": an issue whose review passes stamps a per-run active-<N>-YYYYMMDD-HHMM label (UTC; the issue number keeps the repo-level label entity per-run so one run's clear can never strip another's hold), its Touches line is the claimed file set, holds refresh on three events a run actually performs (review dispatch, commit, push) and never on clocks, and a stamp over 36 hours old, or over an hour in the future, grants no hold and any run may clear it, deleting the entity. The size rule is a four-branch decision on change size (larger of insertions or deletions, the review-size bound's unit) and held/free: small-in-held is permitted with the PR body naming the holder, small-in-free and large-in-free claim the file via a size-rule Touches record (a second sanctioned record type beside the disposition-1 widening), large-in-held waits, with a double-claim tie-break (later stamp yields; equal stamps, higher issue number yields). Every file that commanded the note-only disposition now points at the home instead of restating it: the protocol's Finding disposition and both worked examples, decision-heuristics scope discipline, the capture-system-defect skill, reviewer-issue, reviewer-pr's changed-files row, implementation-agent's Noticed field, orchestrator's fix-it-first rules and pipeline wiring, build.md, post-wave-review (deliberately out of scope, read-only), and CLAUDE.md's label list. Decisions are mechanized in tools/file-claim-core.ps1 (registered in governance-manifest.json sharedPaths/classes as content) and pinned by tests/file-claim.test.js, 32 cases including malformed labels, staleness boundaries, mislabeled and future-dated stamps, backslash normalization, and the yield order; design rationale recorded in DESIGN.md. Issue review: FAIL x5 (verification scoping, missing note-command homes, ownership wall, mid-run Touches growth, shared label entity) then PASS with four minors folded in. Code round 1: reviewer-pr FAIL (2 majors, two worked examples still commanding notes), reviewer-design-philosophy FAIL (1 major, threshold and label shape restated outside the home), reviewer-architecture FAIL (1 blocker, the new tool in neither manifest list would red CI; 1 major, no DESIGN.md record), briefing audit PASS. Round-2 scoped re-check confirmed all five fixed and found 1 new major (the future-stamp liveness rule living only in code); round-3 scoped re-check PASS, briefing audit PASS. Gates at ship: npm test 261/261, format and em-dash checks clean. CI expectation: build green on PR #47.
