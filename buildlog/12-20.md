- 2026-08-22 - #12 Took the right to file an issue away from every agent and gave the owner one priced report instead (PR #20): agents keep the right to notice, fix what they can inside the work they were given, carry what they cannot as a note, and the session ends with four options priced cheapest first (Nothing, Delete, Small, Big) whose percentages sum to 100, never a pick made on the owner's behalf. The rule gets one home in agents/orchestrator.md; the other seventeen files are deletions or redirects. Ten carried a live instruction to file, two of them hooks whose reason strings enter the model's context directly and so outranked any markdown: loop-gate.ps1 told an overnight run to file issues on every forced turn, and goal-gate.ps1 narrowed what could be surfaced at all so tightly that a run-blocking defect had nowhere to go. Four more were the missing channel or the missing moment, a note having no way home from a spawned agent and no defined place in the pipeline to be delivered. Notes persist in the session's gitignored .run_state/notes.md, appended on receipt, never truncated, and carried across a governance-sync worktree re-cut that would otherwise abandon them; the report fires when the session stops working, on a wrap, a ship, or a halt, and a session holding no notes says so in one line. Nothing was added: eighteen M entries, zero A, no new tracked file, skill, tool, script, hook, or CI check. Issue review: PASS on round 14, DENY sonnet-only, with the file count moving 1 to 5 to 10 to 17 as each round found another home for the old rule, then back down when the reviewer itself argued for fewer criteria. Owner-directed widening recorded on the issue: agents/reviewer-design-philosophy.md joined Touches on the owner's explicit call after five rounds failed on one line, because that reviewer's output contract admitted only a verdict and numbered findings, so a pre-existing instance either deadlocked the gate or vanished; it now carries a Report notes: block that does not affect its verdict. Code review: PASS from reviewer-pr, with reviewer-design-philosophy and reviewer-architecture gating round 1 and the briefing audit PASS on all six rounds. Process defect found mid-run and corrected, recorded here because it cost more than any finding: rounds 2 through 5 were run as full three-reviewer re-reviews of the whole artifact, while standards/adversarial-review-protocol.md specifies one fresh reviewer scoped to the fix for rounds 2+ and says minor and nit findings ship inline with no re-review; running the wrong loop manufactured most of the churn, and switching to the specified one converged the change. The segment was halted once under the stop rule, logged, and resumed only on the owner's direction to widen scope. Recorded omissions: DESIGN.md gets no ADR (owner's call), each child repo's own CLAUDE.md is unreachable from here, definition-of-done.md's wanting-can-wait phrasing needs owner approval to touch, the owner's global CLAUDE.md was corrected out of band and verified on disk, and Plan step 4's live-log ledger copy was dropped as a second home for one note. Gates at ship: npm test 132 passed across 8 files, check-emdash clean. CI expectation: the build workflow green on PR #20.
