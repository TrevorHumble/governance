{
  "name": "governance",
  "private": true,
  "version": "1.0.0",
  "description": "Parent governance repo: standards, agent definitions, pipeline commands, hooks, and the checks that enforce them.",
  "scripts": {
    "test": "vitest run",
    "buildlog": "node scripts/buildlog-glue.js",
    "check:emdash": "node scripts/check-emdash.js",
    "lint": "eslint . --max-warnings=0",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "vitest": "^4.1.11"
  }
}
